# These are a bare minimum commands required to get started with the tests
kubectl apply -f quick-test-namespace.yaml
kubectl label nodes k8s-worker-1 testcapability=testnode

# Before running pods, please clone this repo in the first worker node and
# create the required docker image by running the instructions in image/README

# Go to headless-service-test directory to try out headless service discovery test
# Go to tcp-service-test directory to try out TCP ClusterIP sercvice test
# Go to configmap directory to try out various ways of pushing config details to containers
